module.exports = {
    apps: [
        {
            name: "ui-mockup",
            script: "npm",
            args: "run dev",
            env: {
                NODE_ENV: "development"
            }
        }
    ]
}